"""
    Calculation module
"""


def add(x: int, y: int) -> int:
    """Adding 2 arguments, returning a sum

    Args:
        x (int): first argument
        y (int): second argument

    Returns:
        int: sum of adding 2 arguments
    """
    return x + y
